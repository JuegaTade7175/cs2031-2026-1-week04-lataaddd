version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: week04-db
    ports:
      - "5436:5432"
    environment:
      POSTGRES_DB: week04-db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres